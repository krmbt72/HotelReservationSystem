#include "reservation.h"
/*
unsigned int reservationNo = 0;

Reservation::Reservation(Customer customer,Room room,QDate date)
{
    customers.push_back(customer);
    rooms.push_back(room);
    dates.push_back(date);
    reservationNo++;
}

std::vector<Customer> Reservation::getCustomers()
{
    return customers;
}

std::vector<Room> Reservation::getRooms()
{
    return rooms;
}

std::vector<QDate> Reservation::getDates()
{
    return dates;
}

void Reservation::deleteCustomer(Customer customer)
{

    for(std::vector<Customer>::iterator it = customers.begin(); it!=customers.end(); it++ )
    {
        if(it->getFullname() == customer.getFullname() && it->getAge()== customer.getAge() &&
            it->getGender() == customer.getGender())
            customers.pop_back();
    }
}
void Reservation::addCustomer(Customer _customer){

}
void Reservation::checkOut(Room room)
{

}
*/
